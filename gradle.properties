# Any property can be overwritten from command-line via
#   -P<propertyName>=<value>

DotnetPluginId=ReSharperPlugin.RimworldDev
DotnetSolution=ReSharperPlugin.RimworldDev.sln
RiderPluginId=com.jetbrains.rider.plugins.rimworlddev
PluginVersion=2025.1.11

BuildConfiguration=Release

PublishToken="_PLACEHOLDER_"

# The Rider version is <SdkVersion> in Directory.Build.props; build.gradle.kts derives the IntelliJ Platform
# ProductVersion (e.g. 2026.3-EAP2-SNAPSHOT) from it. -PProductVersion=... overrides the derived value.

# Kotlin 1.4 will bundle the stdlib dependency by default, causing problems with the version bundled with the IDE
# https://blog.jetbrains.com/kotlin/2020/07/kotlin-1-4-rc-released/#stdlib-default
kotlin.stdlib.default.dependency=false
org.gradle.jvmargs=-Xmx4g

# Required to download Rider artifacts from Maven (and not "binary" releases from CDN).
org.jetbrains.intellij.platform.buildFeature.useBinaryReleases=false

org.gradle.configuration-cache=true